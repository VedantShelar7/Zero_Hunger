{
  "name": "backend",
  "version": "1.0.0",
  "description": "ZeroHunger Backend - MongoDB Edition",
  "main": "server.js",
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "start": "node server.js",
    "build": "npm install",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "mongoose": "^8.9.0",
    "dotenv": "^16.4.5"
  }
}
